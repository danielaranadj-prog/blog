---
import BaseHead from '../components/BaseHead.astro';
import BlogHeader from '../components/BlogHeader.astro';
import Footer from '../components/Footer.astro';
import { SITE } from '../config';

let title = "Manifiesto | " + SITE.title;
let description = "No somos agencia. Somos viajeros reales compartiendo Trips & Tips.";
let permalink = 'https://blog.instantetrips.com/manifiesto';
---

<html lang="es">
<head>
    <BaseHead {title} {description} {permalink} />
    <style>
        /* === MANIFESTO PAGE - PREMIUM UPGRADE === */
        :root {
            --brand-blue: #75AADB;
            --accent-gold: #F4B942;
            --page-bg: #F8F9FA;
            --text-main: #2D3748;
            --text-muted: #718096;
            --card-border: #E2E8F0;
        }

        @media (prefers-color-scheme: dark) {
            :root {
                --page-bg: #0f172a;
                --text-main: #f1f5f9;
                --text-muted: #94a3b8;
                --card-border: #334155;
            }
        }

        body {
            background-color: var(--page-bg);
            color: var(--text-main);
            margin: 0;
            transition: all 0.3s ease;
        }

        .manifesto-container {
            max-width: 800px;
            margin: 4rem auto;
            padding: 0 1.5rem;
            position: relative;
        }

        /* Decorative element */
        .manifesto-container::before {
            content: '"';
            position: absolute;
            top: -2rem;
            left: 1rem;
            font-size: 8rem;
            font-family: 'Georgia', serif;
            color: var(--accent-gold);
            opacity: 0.1;
            line-height: 1;
            pointer-events: none;
        }

        .manifesto-header {
            text-align: center;
            margin-bottom: 4rem;
            animation: fadeInUp 1s ease-out;
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .manifesto-title {
            font-family: 'Montserrat', sans-serif;
            font-size: clamp(2rem, 5vw, 3.5rem);
            font-weight: 800;
            margin-bottom: 1.5rem;
            line-height: 1.15;
            text-transform: uppercase;
            background: linear-gradient(
                135deg, 
                var(--text-main) 0%, 
                var(--accent-gold) 50%, 
                var(--text-main) 100%
            );
            background-size: 200% auto;
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: shimmer 6s linear infinite;
        }

        @keyframes shimmer {
            0% { background-position: -200% center; }
            100% { background-position: 200% center; }
        }

        .manifesto-subtitle {
            font-size: 1.15rem;
            color: var(--brand-blue);
            font-family: 'IBM Plex Serif', serif;
            font-style: italic;
            opacity: 0;
            animation: fadeInUp 1s ease-out 0.3s forwards;
        }

        .manifesto-content {
            font-family: 'IBM Plex Serif', serif; 
            font-size: 1.25rem;
            line-height: 1.85;
            color: var(--text-main);
        }

        .manifesto-content p {
            margin-bottom: 2rem;
            opacity: 0;
            animation: fadeInUp 0.8s ease-out forwards;
        }

        .manifesto-content p:nth-child(1) { animation-delay: 0.1s; }
        .manifesto-content p:nth-child(2) { animation-delay: 0.2s; }
        .manifesto-content p:nth-child(3) { animation-delay: 0.3s; }
        .manifesto-content p:nth-child(4) { animation-delay: 0.4s; }
        .manifesto-content p:nth-child(5) { animation-delay: 0.5s; }
        .manifesto-content p:nth-child(6) { animation-delay: 0.6s; }
        .manifesto-content p:nth-child(7) { animation-delay: 0.7s; }

        .highlight {
            background: linear-gradient(120deg, rgba(244, 185, 66, 0.25) 0%, rgba(244, 185, 66, 0.25) 100%);
            padding: 2px 8px;
            border-radius: 4px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .highlight:hover {
            background: linear-gradient(120deg, rgba(244, 185, 66, 0.4) 0%, rgba(244, 185, 66, 0.4) 100%);
        }

        /* Signature block premium */
        .signature-block {
            margin-top: 5rem; 
            padding-top: 2.5rem;
            border-top: 2px solid transparent;
            border-image: linear-gradient(90deg, transparent, var(--accent-gold), var(--brand-blue), transparent) 1;
            
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1.5rem;
            
            opacity: 0;
            transform: translateY(40px);
            transition: opacity 1s ease-out, transform 1s cubic-bezier(0.2, 0.8, 0.2, 1);
        }

        .signature-block.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .signature-logo {
            width: 90px; 
            height: auto;
            transition: all 0.4s ease;
            filter: drop-shadow(0 4px 12px rgba(244, 185, 66, 0.2));
        }

        .signature-logo:hover {
            transform: scale(1.1);
            filter: drop-shadow(0 8px 20px rgba(244, 185, 66, 0.4));
        }

        .team-sig {
            font-family: 'Montserrat', sans-serif;
            font-weight: 500; 
            text-transform: none; 
            font-size: 1rem;
            color: var(--text-muted);
            letter-spacing: 0.5px;
            padding: 0.5rem 1.5rem;
            border: 1px solid var(--card-border);
            border-radius: 50px;
            background: rgba(244, 185, 66, 0.05);
            transition: all 0.3s ease;
        }

        .team-sig:hover {
            border-color: var(--accent-gold);
            background: rgba(244, 185, 66, 0.1);
            color: var(--text-main);
        }

        @media (prefers-color-scheme: dark) {
            .highlight { 
                background: linear-gradient(120deg, rgba(117, 170, 219, 0.2) 0%, rgba(117, 170, 219, 0.2) 100%); 
            }
            .highlight:hover {
                background: linear-gradient(120deg, rgba(117, 170, 219, 0.35) 0%, rgba(117, 170, 219, 0.35) 100%);
            }
            .logo-filter { filter: invert(1) brightness(2); }
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Serif:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
</head>

<body>
    <BlogHeader />

    <main class="manifesto-container">
        
        <div class="manifesto-header">
            <h1 class="manifesto-title">NO VENDEMOS NADA.<br>VIAJAMOS.</h1>
            <p class="manifesto-subtitle">Una declaración para el que no quiere ir a ciegas.</p>
        </div>

        <div class="manifesto-content">
            <p>
                Aclaremos esto primero: <strong>No somos una agencia.</strong> No te vamos a vender un tour, ni a prometerte el paquete "todo incluido" perfecto.
            </p>

            <p>
                Nuestro propósito es más simple y, a la vez, más honesto: darte <strong>Trips & Tips</strong>.
            </p>

            <p>
                Viajar a un lugar nuevo es fascinante, pero ir a ciegas puede ser aterrador (o muy caro). A veces investigamos durante horas, otras veces nos lanzamos a la aventura sin mapa. Y en ambos casos, <span class="highlight">nos hemos equivocado para que tú no tengas que hacerlo.</span>
            </p>

            <p>
                Aquí contamos nuestras experiencias reales y nuestros errores. Esos detalles que hacen que llegues a tu destino sabiendo dónde pisar, qué comer y qué trampa de turista evitar.
            </p>

            <p>
                Sabemos que existen dos tipos de personas: los que quieren que una agencia les resuelva la vida y los que necesitan sentir el pulso del lugar. 
            </p>

            <p>
                Nosotros somos exploradores. Somos mochileros. <strong>Somos viajeros, no turistas.</strong>
            </p>

            <p>
                Si eres de los que prefieren descubrir a que se lo cuenten, si buscas la verdad detrás de la postal... entonces estás en casa. <span class="highlight">Bienvenido a tu lugar.</span>
            </p>
        </div>

        <div class="signature-block" id="signature">
            <div class="team-sig">
                El colectivo Instante Trips and Tips
            </div>
            <img src="/logo.svg" alt="Instante Trips" class="signature-logo logo-filter" />
            
        </div>

    </main>

    <Footer />

    <script>
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });

        const signature = document.getElementById('signature');
        if (signature) {
            observer.observe(signature);
        }
    </script>
</body>
</html>